Получено новое сообщение: #{@message.subject.html_safe? ? @message.subject : strip_tags(@message.subject)}
\===============================================
\
Отправитель: #{@message.sender.profile.name} (#{t(:"user.roles.#{@message.sender.role.downcase}")})
\
Текст сообщения:

\-----------------------------------------------
\#{@message.body.html_safe? ? @message.body : strip_tags(@message.body)}
\-----------------------------------------------

Вы можете зайти на сайт (#{new_user_session_url}) и ответить на это сообщение в личном кабинете.
