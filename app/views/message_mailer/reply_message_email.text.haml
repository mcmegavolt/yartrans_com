Новый ответ на Ваше сообщение: #{@message.subject.html_safe? ? @message.subject : strip_tags(@message.subject)}
\===============================================
\
Отправитель: #{@message.sender.profile.name} (#{t(:"user.roles.#{@message.sender.role.downcase}")})
\
Текст ответа:

\-----------------------------------------------
\#{@message.body.html_safe? ? @message.body : strip_tags(@message.body)}
\-----------------------------------------------

Вы можете зайти на сайт (#{root_url}) и ответить на это сообщение в личном кабинете.

